[{:itonami.blueprint/id "cloud-itonami-isic-4510"
  :itonami.blueprint/name "Sale of motor vehicles"
  :itonami.blueprint/isic-rev5 "4510"
  :itonami.blueprint/domain :automotive/vehicle-sales
  :itonami.blueprint/license "AGPL-3.0-or-later"
  :itonami.blueprint/social-impact [:consumer-protection :transparency :road-safety]
  :itonami.blueprint/governor :vehicle-sale-governor
  :itonami.blueprint/robotics false
  :itonami.blueprint/required-technologies [:identity :forms :dmn :bpmn :audit-ledger]
  :itonami.blueprint/optional-technologies []
  :itonami.blueprint/status :public-oss
  :itonami.blueprint/maturity :implemented
  :itonami.blueprint/implemented-slice
  "VehicleSale-LLM ⊣ VehicleSaleGovernor, langgraph-clj StateGraph
  (langgraph.graph, vehiclesale.operation/build: intake -> advise ->
  govern -> decide -+-> commit / request-approval -> commit / hold),
  interrupt-before #{:request-approval} + in-memory checkpointer for
  real human-in-the-loop resume (title officer approves/rejects), and
  an append-only audit ledger (vehiclesale.store/ledger +
  append-ledger!, MemStore + DatomicStore parity). The Governor
  (src/vehiclesale/policy.cljc) runs HARD 16 + SOFT (confidence,
  salvage-title-gate, dispute-request, money-rail). HARD:
  rbac, lien-clearance-gate, odometer-disclosure-gate,
  source-provenance-gate, licensed-disclosure, kobutsusho-license-gate,
  repair-history-disclosure-gate, inquiry-target-gate,
  scan-coverage-gate, shaken-validity-gate, x402-receipt-gate,
  escrow-conservation-gate, payout-destination-gate,
  funds-not-arrived-gate, custody-handover-gate, scope-exclusion-gate.
  The public face is a Carsensor-like JP catalog (vehiclesale.catalog;
  docs/index.html SPA #search / #v/<vin> / #operator) generated by
  clojure -M:dev:render-html from a real actor run. Writes still go
  through the graph. Vehicle purchase is a Stripe separate-charges
  authorisation (execute? false) released after capture + 納車; listing
  info unlocks are x402 via nexus-x402; custody is a VIN status. Compose
  with cloud-itonami-marketplace-settlement — this actor does not execute
  transfers. Provenance is limited to real, citable public reference
  sources (src/vehiclesale/facts.cljc: NMVTIS, NHTSA recalls, MLIT recall
  registry) plus two structural classes, :operator-licensed-dmv-feed
  (state DMV) and :operator-licensed-shakensho-feed (電子車検証 / AIRIS).
  Test suite (clojure -M:dev:test) and clj-kondo (clojure -M:lint) both
  green; governance-doc set (LICENSE, GOVERNANCE.md, CONTRIBUTING.md,
  SECURITY.md, CODE_OF_CONDUCT.md), docs/ (business model, design ADR,
  operator guide) already present."
  :db/id -1}]
